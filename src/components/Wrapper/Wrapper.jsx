import React from 'react';
import Reuseable from '../Reuseable/Reuseable';
import Container from '../Container/Container';

const Wrapper = () => (
  <Reuseable name="Wrapper">
    <Container />
  </Reuseable>
);

export default Wrapper;
